<%= simple_form_for(list) do |f| %>
  <%= f.input :name %>
  <%= f.input :image_url %>
  <%= f.submit class: "btn btn-outline-primary" %>
  <%= f.button :button, ("#{link_to 'Cancel', lists_path, class: 'btn btn-none'}").html_safe, type: :reset %>
<% end %>
