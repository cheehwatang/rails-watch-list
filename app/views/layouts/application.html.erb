<!DOCTYPE html>
<html>
  <head>
    <title>RailsWatchList</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", defer: true %>
  </head>

  <body>
    <nav class="navbar navbar-expand-lg bg-light border border-bottom-2 px-4">
      <div class="container-fluid d-flex justify-content-between align-items-center">
        <%= link_to 'My Watch List', root_path, class: 'navbar-brand text-decoration-none fs-5 text-dark' %>
        <%= link_to 'New List', new_list_path, class: 'btn btn-outline-primary' %>
      </div>
    </nav>
    <%= yield %>
  </body>
</html>
